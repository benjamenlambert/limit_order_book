//
// Created by Benjamen Lambert on 3/24/2020.
//

#include "Event.h"
#include "OrderBook.h"

void Event::Update(const OrderUpdate &update) {
  if (update.action == 'a') {
  } else if (update.action == 'd') {
  } else {
  }
}